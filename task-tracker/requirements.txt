# Core dependencies
typer[all]>=0.9.0
pydantic>=2.0.0

# Ed25519 signing support (for agent_messaging)
cryptography>=46.0.0

# Development dependencies (install with: pip install -r requirements-dev.txt)
